import 'package:flutter/material.dart';

class CepScreenPage extends StatelessWidget {
  final String cep;
  const CepScreenPage({super.key, required this.cep});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
